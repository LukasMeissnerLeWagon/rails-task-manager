<h2><%= @task.title %></h2>
<p><%= @task.details %></p>
<p><%= @task.completed ? "✅" : "❎" %></p>

<%= link_to "See all tasks", tasks_path %>
<%= link_to "Edit task", edit_task_path(@task) %>
<%= link_to "Delete task ☠️", task_path(@task), method: :delete %>
